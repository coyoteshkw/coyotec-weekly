---
/**
 * LinkCard.astro - 链接卡片组件
 *
 * 手动指定标题、描述和缩略图的链接预览卡片。
 * 不自动抓取 OG 信息，所有内容由用户传入。
 *
 * 用法（在 .mdx 文件中）：
 *
 * ```mdx
 * import LinkCard from "@/components/LinkCard.astro"
 *
 * <LinkCard
 *   href="https://example.com"
 *   title="示例网站"
 *   description="这是一个示例网站的简介"
 *   image="https://example.com/thumb.jpg"
 * />
 * ```
 *
 * @prop {string}  href        - 目标链接（必填）
 * @prop {string}  title       - 卡片标题（必填）
 * @prop {string}  [description] - 卡片描述（可选）
 * @prop {string}  [image]     - 缩略图 URL（可选）
 */

export interface Props {
  href: string;
  title: string;
  description?: string;
  image?: string;
}

const { href, title, description, image } = Astro.props;

let hostname = "";
try {
  hostname = new URL(href).hostname;
} catch {
  hostname = href;
}
---

<a
  href={href}
  target="_blank"
  rel="noopener noreferrer"
  class="link-card group relative flex w-full overflow-hidden rounded-xl border border-border bg-background no-underline transition-all duration-300 hover:shadow-lg hover:border-accent/40"
>
  <!-- 缩略图：用 flex 撑满高度 + cover 裁切 -->
  {
    image && (
      <div class="link-card-thumb w-36 shrink-0 self-stretch overflow-hidden sm:w-44">
        <img
          src={image}
          alt={title}
          loading="lazy"
          class="link-card-thumb-img block h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
        />
      </div>
    )
  }

  <!-- 文字区域 -->
  <div class="flex min-w-0 flex-1 flex-col justify-center gap-1.5 p-4">
    <!-- 域名 + favicon -->
    <span class="flex items-center gap-1.5 text-xs text-muted-foreground">
      <img
        src={`https://www.google.com/s2/favicons?domain=${hostname}&sz=16`}
        alt=""
        class="link-card-favicon inline-block size-4 rounded-sm shrink-0"
        width="16"
        height="16"
        loading="lazy"
        onerror="this.style.display='none'"
      />
      <span class="truncate">{hostname}</span>
    </span>

    <!-- 标题 -->
    <span class="line-clamp-2 text-sm font-semibold text-foreground group-hover:text-accent transition-colors">
      {title}
    </span>

    <!-- 描述 -->
    {
      description && (
        <span class="line-clamp-2 text-xs text-muted-foreground">
          {description}
        </span>
      )
    }
  </div>

  <!-- 右下角 ↗ 图标 -->
  <span class="absolute bottom-2 right-2 text-muted-foreground/50 transition-colors group-hover:text-accent/60">
    <svg viewBox="0 0 24 24" class="size-4 fill-current">
      <path d="M7 17L17 7M7 7h10v10" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round" />
    </svg>
  </span>
</a>

<style>
  .link-card {
    margin: 1.5rem 0;
    display: flex;
    width: 100%;
    overflow: hidden;
    border-radius: 0.75rem;
    border: 1px solid var(--color-border);
    background: var(--color-background);
    text-decoration: none;
    transition: all 0.3s;
  }
  .link-card:hover {
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    border-color: color-mix(in srgb, var(--color-accent) 40%, transparent);
  }

  /* 缩略图容器 */
  .link-card-thumb {
    width: 9rem /* 36 */;
    flex-shrink: 0;
    align-self: stretch;
    overflow: hidden;
  }
  @media (min-width: 640px) {
    .link-card-thumb {
      width: 11rem /* 44 */;
    }
  }
  .link-card-thumb-img {
    display: block !important;
    width: 100% !important;
    height: 100% !important;
    object-fit: cover !important;
    margin: 0 !important;
    border-radius: 0 !important;
  }

  /* Favicon */
  .link-card-favicon {
    display: inline-block !important;
    width: 1rem !important;
    height: 1rem !important;
    margin: 0 !important;
    border-radius: 0.125rem !important;
  }

  /* 行数截断 */
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
